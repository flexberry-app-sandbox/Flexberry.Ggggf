import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('transform:i-i-s-ggggf-время', 'Unit | Transform | i i s ggggf время', function(hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it exists', function(assert) {
    let transform = this.owner.lookup('transform:i-i-s-ggggf-время');
    assert.ok(transform);
  });
});
